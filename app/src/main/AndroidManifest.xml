<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.mk.FaNBook" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="app.mk.FaNBook.GuideActivity"
            android:label="Welcome!" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="app.mk.FaNBook.InfoActivity"
            android:label="InfoActivity" >
        </activity>
        <activity
            android:name="app.mk.FaNBook.MainActivity"
            android:label="MainActivity" >
        </activity>
        <activity
            android:name="app.mk.FaNBook.SearchActivity"
            android:label="SearchActivity" >
        </activity>
    </application>

</manifest>
